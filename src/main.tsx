import React from 'react'
import ReactDOM from 'react-dom/client'
import Plum from './Plum.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <Plum />
  </React.StrictMode>,
)
